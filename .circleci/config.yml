version: 2
jobs:
  build:
    docker:
      - image: ubuntu:18.04

    steps:
      - checkout

      - run:
          name: Setup Build Tools
          command: 'sudo apt install build-essential pkg-config qtchooser'
      - run:
          name: Install Libraries
          command: 'sudo apt install qt5-default libqt5svg5-dev qtmultimedia5-dev \
          libavutil-dev libavformat-dev libavcodec-dev libavfilter-dev libavutil-dev libswscale-dev'

      - run:
          name: Print the Current Time
          command: date